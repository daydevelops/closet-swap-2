before(() => {
    // login
    // seed some users
    // seed items for those users
});

describe('Testing Profile Viewing', () => {
    it('allows a user to start a chat with a user', () => {
        // if I view a users profile
        // then I should see a chat button
        // clicking the button should direct me to a chat with the user
    });

    it('allows a user to see the users closet', () => {
        // if I view a users profile
        // then I should see a list of their recent items
    });

    it('allows a user to filter the users closet', () => {
        // if I view a users profile
        // and I filter by category
        // then I should see a list of items only in that category
    });

    it('allows a user to search the users closet', () => {
        // if I view a users profile
        // and I search for a specific item by title
        // then I should see only items that match my search
    });

    it('allows a user to friend the user', () => {
        // if I view a users profile
        // and I click on the friend button
        // then the user should be added to my friends list
    });

    it('allows a user to block the user', () => {
        // if I view a users profile
        // and I click on the block button
        // then the user should be added to my blocked list
    });

    it('allows a user to like an item', () => {
        // if I view a users profile
        // and I click on the like button for an item
        // then the item should be added to my liked items
    });

    it('allows a user to start a chat for an item', () => {
        // if I view a users profile
        // and I click on an item
        // then I should see a chat button
        // clicking the button should direct me to a chat with the item owner
    });

});
