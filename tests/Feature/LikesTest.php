<?php

test('a user can like a user', function () {
    $this->assertTrue(0);
});

test('a user can unlike a user', function () {
    $this->assertTrue(0);
});

test('a user can see all users that have liked them', function () {
    $this->assertTrue(0);
});

test('a user can see all users that they have liked', function () {
    $this->assertTrue(0);
});

test('a user can see all users that have liked their clothing item', function () {
    $this->assertTrue(0);
});

test('a user can like a clothing item', function () {
    $this->assertTrue(0);
});

test('a user can unlike a clothing item', function () {
    $this->assertTrue(0);
});
