<?php

test('anyone can see the donations page', function () {
    $this->assertTrue(0);
});

test('a user can donate', function () {
    $this->assertTrue(0);
});

test('if an authenticated user donates, they get a badge', function () {
    $this->assertTrue(0);
});
